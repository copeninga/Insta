import 'package:flutter/material.dart';
import 'pacakge:insta/insta_list.dart';



class Instabody extends StatelessWidget {
  @override
  Widget build(BuildingContext context) {
    return new Column(
      children: <Widget>[
        Flexible(child: InstaList(),)
        ], // <Widget>[]
        ); // Column 
        }

        }

        
        
